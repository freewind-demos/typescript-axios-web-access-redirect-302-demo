TypeScript Axios Web Access Redirect 302 Demo
===========================

在浏览器中，axios也无法拿到302返回的location。`maxRedirects:0`无效，会自动跳转。

```
npm install
npm start
```

It will open page on browser automatically.
